[tool.ruff]
target-version = "py311"
line-length = 88
extend-exclude = ["models", ".local-history", "backups", ".venv"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"notebooks/*.ipynb" = ["E402"]
